<div class="container-field">
  <div class="row justify-content-center">
    <div class="col-xl-10 col-lg-12 col-md-12 col-sm-12">
      <div class="border contents-area bg-light">

        <div class="text-center mt-5"><h3><strong><%= flash[:notice] %></strong></h3>
          <h1><strong class="border-bottom border-dark">投稿一覧</strong></h1>
            <%= link_to "新しい順", posts_path(latest: "true") %>
            |<%= link_to "古い順", posts_path(old: "true") %>
        </div>

            <%= render "search", post: @post %>
              <br>
            <%= render "index", post: @post %>

          <div class="pagination justify-content-center mt-3">
            <%= paginate @posts, theme: "bootstrap-5" %>
          </div>

      </div>
    </div>
  </div>
</div>